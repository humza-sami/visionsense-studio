# Optional runtime overrides (copy to .env; docker-compose reads it).
# MODEL_WEIGHTS=models/yolo26n.pt
# MODEL_DEVICE=auto            # auto | cpu | mps | cuda:0
# REDIS_ENABLED=true
# REDIS_HOST=redis
# API_PORT=8000
